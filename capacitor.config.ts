import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "cn.zhangruijunlaoshi.rundown",
  appName: "新媒体串单",
  webDir: "pages-dist",
  server: {
    androidScheme: "https",
  },
};

export default config;

